# Phase P15 Completion

Phase: `PLAN-20260723-SELFHOST-RELIABILITY.P15`
Completed: 2026-07-23
Verdict: PASS

Implemented additive migration and deprecation:

- historical runs without a valid pre-execution V1 capsule are permanently
  salvage-only; no capsule backfill path exists;
- immutable append-only salvage lineage records audit refused exact recovery;
- legacy checkpoint rows remain readable and are not rewritten or deleted;
- fresh exact recovery uses durable recovery attempts and operation epochs;
- operator resume/retry/rewind, daemon resume, and child resume execute only
  through RecoveryProtocolV1 with RunnerRecoveryExecutor;
- caller-controlled trusted_internal authorization was removed;
- normal and recovered execution share one runner transition loop and recovery
  persists the exact live StateSnapshot rather than a synthetic default.

No production resume entrypoint calls commit_continuation or uses a legacy
reconstructed-runner fallback.
