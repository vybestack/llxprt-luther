# Phase P17 Completion

Phase: `PLAN-20260723-SELFHOST-RELIABILITY.P17`
Completed: 2026-07-23
Verdict: PASS

Implemented typed, strategy-aware verified merge and the first durable writer
of `RunStatus::Merged`.

- Added nonterminal, nonresumable `ReviewReady` as the fixed predecessor.
- Added immutable `merge_artifacts` persistence initialized with the database.
- Added injected Git/remote probes and strategy-specific merge, squash, and
  rebase evidence.
- Exact base commit is resolved from the capsule base ref; empty identities fail
  closed.
- Merge strategy is declared in capsule-backed configuration; production never
  guesses a strategy.
- External evidence is computed before a short IMMEDIATE transaction.
- The transaction revalidates run repository, PR, head, and capsule identity,
  then atomically inserts the exact artifact and transitions ReviewReady to
  Merged with an affected-row CAS.
- Completion requires both the artifact and Merged status.
- Dogfood now declares squash merge, waits through the existing PrMerge durable
  wait path, reaches ReviewReady, and invokes production typed completion.
