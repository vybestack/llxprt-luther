# Phase P16 Completion

Phase: `PLAN-20260723-SELFHOST-RELIABILITY.P16`
Completed: 2026-07-23
Verdict: PASS

Implemented a deterministic real-SQLite/tempdir failpoint matrix with one test
for each F1-F14. Every test asserts both a typed recovery outcome and durable
state invariant. The matrix covers atomic launch rollback, pre-step recovery,
ContinueWorkspace verification, commit/push effect reconciliation, stale epoch,
exact duplicate idempotency, capsule tampering, ownership TOCTOU, base binding,
legacy salvage, two-connection epoch racing, conflicting operations, and crash
adoption between execute and finalize.

The matrix found and fixed a real source-attempt bug: prepare selected the
currently unfinalized recovery attempt as its own source, changing the logical
operation key and preventing expired pending-operation adoption. Source
selection now uses only finalized attempts. F9 also removes both bootstrap and
durable ownership evidence, matching the production ownership model.
