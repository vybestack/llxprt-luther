# Phase P10 Completion

Phase: `PLAN-20260723-SELFHOST-RELIABILITY.P10`
Completed: 2026-07-23
Verdict: PASS (RED phase verified)

Created `tests/recovery_protocol_integration_tests.rs` with **32 behavioral
integration-first tests** exercising `RecoveryProtocolV1::recover()` and the
step recovery policy/strategy selection through the **public API** against real
durable SQLite stores (epoch CAS, operations ledger, append-only attempts,
immutable capsule store). No in-memory facade, no private-field construction,
no mocks that bypass authority. [C1/C2/C3/C4/C5/C6/C8/B1/B2/B3/B4/B6/B7]

## RED/Green Split (valid red exclusively at designated P11 behavior)

**16 RED tests (failing at designated P11 stub/todo!()):**

- 12 tests calling `RecoveryProtocolV1::recover()` fail at `todo!()` panic at
  `src/engine/recovery/protocol.rs:424` (designated P11 recover behavior):
  - `fresh_recovery_with_valid_epoch_returns_recovered` [C5/B2]
  - `stale_epoch_returns_stale_epoch_outcome` [C1/B2]
  - `completed_duplicate_returns_already_applied` [C2]
  - `conflicting_duplicate_binding_is_refused` [C2/B3]
  - `pending_duplicate_is_reconciled_not_duplicated` [C2/B3]
  - `epoch_cas_advances_and_re_recovery_returns_already_applied` [C1/C12/B2]
  - `protocol_returns_recovered_only_after_finalize` [C5/C12]
  - `authority_changed_between_prepare_and_reserve_returns_error` [B1]
  - `continue_workspace_with_matching_verification_returns_recovered` [C4]
  - `continue_workspace_mismatched_worktree_returns_refused` [C4]
  - `continue_workspace_toctou_mismatch_returns_not_authorized` [C4/B6]
  - `recovery_loads_immutable_capsule_with_verified_envelope_digest` [C8]

- 4 tests asserting the fail-closed policy/strategy stubs return the expected
  P11 behavior (they currently return NonRecoverable/Refused):
  - `canonical_continue_workspace_step_returns_continue_workspace` [C6/B7]
  - `safe_rerun_step_returns_idempotent` [C6]
  - `select_strategy_for_continue_workspace_returns_continue_workspace` [C4/C6]
  - `select_strategy_for_idempotent_returns_reenter` [C4/C6]

**16 GREEN tests (passing — durable store invariants + structural assertions):**

Durable store tests (real SQLite, P05/P08 implementations):
- `epoch_cas_stale_rejects_with_persisted_value` [C1/B2]
- `expired_lease_pending_is_adoptable_by_second_recoverer` [B3]
- `durable_attempt_start_recorded_at_reserve_before_execute` [B4]
- `crash_recovery_loads_unfinalized_attempt` [B4]
- `finalize_does_not_advance_epoch_single_cas_at_reserve` [B2]
- `capsule_store_rejects_overwrite_preserving_original` [C8]
- `operations_ledger_enforces_logical_request_uniqueness` [C2/B3]
- `live_lease_pending_refuses_adoption` [B3]

Fail-closed stub verification (demonstrating fail-closed defaults hold):
- `generic_shell_step_without_declaration_is_non_recoverable` [C6]
- `unknown_step_id_is_non_recoverable` [C6]

Structural invariant verification:
- `recovery_request_has_no_authorization_bool` [C4/B2]
- `already_applied_outcome_carries_all_fields` [C2/B3]
- `stale_epoch_outcome_carries_persisted_and_expected` [C1/B2]
- `refusal_reason_includes_conflicting_operation_and_not_authorized` [C2/C4]
- `recovery_error_includes_authority_changed_and_revoked` [B1/B6]
- `canonical_continue_workspace_step_def_carries_policy` [C6/B7]

## Verification

- `cargo build --all-targets` succeeds.
- `cargo clippy --all-targets --all-features -- -D warnings` succeeds (zero warnings).
- `cargo fmt --check` succeeds.
- 33 `@plan:PLAN-20260723-SELFHOST-RELIABILITY.P10` markers (exceeds 14+ requirement).
- No `#[should_panic]`, no reverse testing, no `catch_unwind` swallowing red.
- RED log captured: `artifacts/p10-red-log.txt`.

## Production Changes

None. No production code modified. The P09 stub's `recover()` todo!(),
`policy_for_step` fail-closed default, and `select_strategy` fail-closed
default are the designated P11 behavior surfaces. Tests compile against the
existing public API.
