# Phase P13 Completion

Phase: `PLAN-20260723-SELFHOST-RELIABILITY.P13`
Completed: 2026-07-23
Verdict: PASS (RED)

Created `tests/capsule_wiring_integration_tests.rs` with 13 integration-first
real-SQLite/tempdir tests for the actual P08B/P12 architecture:

## GREEN tests (10) — already-implemented invariants

1. `fresh_launch_atomic_pair_persists_run_and_capsule` — P08B atomic fresh
   launch via `EngineRunner::with_db_path_for_launch` persists run + capsule.
2. `fresh_launch_collision_leaves_neither_row_duplicated` — P08B rollback on
   run collision (no orphan capsule).
3. `recovery_wiring_adapter_for_resume_dispatches_object_safe_v1` — P12
   wiring seam dispatches object-safe `Box<dyn CapsuleAdapter>`.
4. `tampered_envelope_digest_fails_verification` — envelope digest verification
   rejects tampering; persist fails; no row left.
5. `unknown_schema_version_rejected_before_adapter_dispatch` — fail-closed
   dispatch for `schema_version = 99`.
6. `current_schema_version_dispatches_v1_adapter` — supported version
   dispatches correctly.
7. `capsule_less_historical_run_fails_closed_on_load` — load fails for a
   capsule-less run.
8. `capsule_less_run_protocol_fails_closed_not_fabricated` — protocol returns
   a hard error (never `Recovered`) for a capsule-less run.
9. `non_recoverable_step_refusal_never_invokes_executor` — refused strategy
   short-circuits before execute; executor never called.
10. `stale_epoch_short_circuit_never_invokes_executor` — stale-epoch
    short-circuit before execute; executor never called.

## RED tests (3) — designated P14 stubs only

1. `v1_adapter_build_instance_reconstructs_instance_from_capsule` — fails at
   `V1Adapter::build_instance` P14 stub.
2. `recovery_wiring_runner_executor_executes_step` — fails at
   `RunnerRecoveryExecutor::build_resume_runner` P14 stub.
3. `production_runner_executor_through_protocol_reaches_recovered` — fails at
   `build_resume_runner` P14 stub through the full protocol.

All RED failures reach ONLY the designated P14 stubs. No production fake
success is fabricated. No `should_panic`. No external GitHub/network access.
Red log captured under `artifacts/p13-red-log.txt`.
