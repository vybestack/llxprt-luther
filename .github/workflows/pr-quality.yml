name: PR Quality

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pr-quality-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  fmt:
    name: Format (rustfmt)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: workflow
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt

      - name: Cache cargo + target
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: workflow -> target

      - name: Check formatting
        run: cargo fmt --all -- --check

  lint:
    name: Lint (clippy + structural)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: workflow
    env:
      CLIPPY_CONF_DIR: .github/clippy
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy

      - name: Cache cargo + target
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: workflow -> target

      - name: Run clippy strict profile
        run: cargo clippy --workspace --all-targets --all-features -- -D warnings

      - name: Enforce no runtime stubs/TODO markers
        run: cargo xtask guard

      - name: Enforce complexity and file-size gates
        run: cargo xtask complexity --changed origin/${{ github.base_ref }} HEAD

  test:
    name: Tests (lib + integration)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: workflow
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo + target
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: workflow -> target

      - name: Run tests
        run: cargo test --workspace --all-features --lib --tests

  gate-a-reachability:
    # Measures whether Luther can carry an issue to a draft PR, by running the
    # shipping binary against a local remote and a fail-closed GitHub stand-in.
    #
    # This check is GREEN while the observed result matches the expectation
    # recorded in tests/gate_a_reachability.rs -- which is currently FAIL. It
    # turns RED when the product's reachability changes in EITHER direction, so
    # an unexplained improvement is surfaced rather than silently absorbed.
    name: Gate A-R reachability (expected FAIL)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: workflow
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo + target
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: workflow -> target

      # No --locked: Cargo.lock is intentionally untracked in this repository,
      # so --locked fails before compiling anything.
      - name: Measure Gate A-R
        run: cargo test --test gate_a_reachability -- --nocapture
        timeout-minutes: 20

  coverage:
    name: Coverage (llvm-cov gate)
    runs-on: ubuntu-latest
    needs:
      - fmt
      - lint
      - test
    defaults:
      run:
        working-directory: workflow
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust toolchain + llvm tools
        uses: dtolnay/rust-toolchain@stable
        with:
          components: llvm-tools-preview

      - name: Cache cargo + target
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: workflow -> target

      - name: Install cargo-llvm-cov
        uses: taiki-e/install-action@v2
        with:
          tool: cargo-llvm-cov

      - name: Run coverage gate
        run: cargo coverage

      - name: Upload coverage summary
        uses: actions/upload-artifact@v4
        with:
          name: workspace-summary
          path: workflow/target/llvm-cov-target/workspace-summary.json

  docs:
    name: Docs (cargo doc)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: workflow
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo + target
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: workflow -> target

      - name: Build documentation
        run: cargo doc --workspace --all-features --no-deps

  security:
    name: Security (cargo audit)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: workflow
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo + target
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: workflow -> target

      - name: Install cargo-audit
        uses: taiki-e/install-action@v2
        with:
          tool: cargo-audit

      - name: Run RustSec audit
        run: cargo audit

  release-readiness:
    name: Release readiness (release build)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: workflow
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo + target
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: workflow -> target

      - name: Build release binary
        run: cargo build --release --bin luther-workflow
